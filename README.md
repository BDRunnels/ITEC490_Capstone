Create admin button that will wipe the database of data. functionality exists in admin.py
1) login as admin
2) wipe everything but the command log

Create kill agent button (in template), kill-script
Create download agent button, in admin.py


